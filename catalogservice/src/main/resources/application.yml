graphql:
  servlet:
    mapping: /catalog
    enabled: true
    corsEnabled: true

graphiql:
  mapping: /graphiql
  endpoint: /catalog
  enabled: true
  pageTitle: GraphiQL
  cdn:
    enabled: false

spring:
  kafka:
    consumer:
      bootstarp-servers: localhost:2181
      group-id: "catalog_consumer"
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer


